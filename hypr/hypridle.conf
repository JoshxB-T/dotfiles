#general {
#    lock_cmd = pidof hyprlock || hyprlock
#    before_sleep_cmd = hyprlock
#}

#listener {
#    timeout = 300
#    on-timeout = notify-send "Convergence is starting..." && sleep 10 && hyprlock
#}

#listener {
#    timeout = 600
#    on-timeout = systemctl suspend
#}
